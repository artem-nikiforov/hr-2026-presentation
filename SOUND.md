# Звук показа

Все звуковые указания взяты из колонки «Примечания / Режиссура» исходного
сценария. Показ звучит уже сейчас: там, где записи нет, играет синтез.
Как только файл появился в папке — он побеждает, править код не нужно.

## Куда класть

```
audio/sfx/<имя>.mp3      акценты — короткие, 0,2–2 с
audio/music/<имя>.mp3    подложка — зацикленная, от 30 с
audio/vo/<id>.mp3        озвучка — уже разложена командой node import_voice.mjs
```

Уровень выставлен в коде, нормализовать записи не нужно. Формат: MP3,
44,1 кГц, моно для акцентов, стерео для музыки.

## Что искать записями

Синтез это не вытянет — нужен настоящий звук.

| Файл | Сцена | Указание в сценарии | Что искать |
| --- | --- | --- | --- |
| `sfx/crunch.mp3` | s01 | «Звук: Хруст бургера» | укус бургера, хруст булки с кунжутом, 0,5–1 с |
| `sfx/rewind.mp3` | s01 → s02 | «резкий звук перемотки плёнки» | tape rewind, film reel rewind, 0,5–1 с |
| `sfx/cricket.mp3` | s04 | «(Звук сверчка)» | одинокий сверчок, комедийная пауза, 1,5–2,5 с |
| `sfx/bricks.mp3` | s10 | «Звук кладки кирпичей или загрузки уровня в игре» | кладка кирпича либо игровой «level build», 1–2 с |
| `sfx/tada.mp3` | s06 | «Тадам!» | фанфара-тадам, 1–1,5 с |
| `music/drive.mp3` | s02 | «Ритмичная, с лёгким басом. Картинки меняются в такт» | ~100–110 BPM, лёгкий бас, без вокала |
| `music/epic.mp3` | s04 | «резкий переход на эпичную музыку» | нарастающая, с ударными |
| `music/confident.mp3` | s09 | «Возвращается бодрая, уверенная музыка» | ровная, деловая, без надрыва |
| `music/warm.mp3` | s13 | «более тёплая, вдохновляющая, но с драйвом» | тёплая, светлая, с движением |

Где брать под конференцию (нужна лицензия, разрешающая показ):

- **freesound.org** — фильтр по лицензии CC0. Лучший источник для хруста,
  сверчка, перемотки и кирпичей.
- **pixabay.com/sound-effects** и **pixabay.com/music** — своя лицензия,
  коммерческое использование разрешено, атрибуция не требуется.
- **mixkit.co/free-sound-effects** — бесплатно, без атрибуции.
- **uppbeat.io** — музыка, бесплатный тариф с атрибуцией.

Скачивать в MP3, обрезать тишину по краям. Для музыки важно, чтобы кусок
зацикливался без щелчка: резать по такту.

## Что уже звучит синтезом

Записи для них не нужны — эти звуки короткие и служебные, синтез даёт
чистый результат и каждый раз чуть другой, поэтому повтор не отдаёт копией.

`tick` (счётчик цифр), `step` (ступень лесенки), `scratch` (скретч пластинки
на «НО…»), `swipe` (уведомление уезжает), `nosignal` (нет связи), `handoff`
(опыт из рук в руки), `cash` (касса на цифрах), `sparkle` (ИИ), `liftoff`
и `rocket` (самолёт в полёте и ракеты), `beat` (удар в такт), `mosaic`
(лица складываются), `warm` (тёплый аккорд финала).

Любой из них тоже можно заменить записью — просто положить файл с тем же
именем в `audio/sfx/`.

## Как ведёт себя музыка

Подложка меняется только там, где это сказано в сценарии:

```
s02  включается drive        ритмичная, с лёгким басом
s04  сменяется на epic       после ухода бренда
s07  ОБРЫВ                   «Музыка обрывается» перед «НО…»
s09  включается confident    бодрая, уверенная
s13  сменяется на warm       тёплая, вдохновляющая
```

Обрыв на s07 — жёсткий, без затухания: это смысловая пауза, она должна
резать. Остальные переходы идут через плавное затухание за 0,9 с.

Под каждой репликой музыка автоматически приглушается и возвращается,
когда голос замолчал.

## Проверка

`npm start`, открыть `http://localhost:8777/`, нажать «Начать показ».
Слушать на тех же колонках, что будут на конференции: на ноутбуке бас
подложки не слышен, и уровень покажется другим.
